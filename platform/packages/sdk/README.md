# SDK Package

This package will hold:

- generated types from shared schemas
- API clients for the control plane
- event publishing helpers
- evidence publishing helpers

It exists so the platform can share the same control contracts across apps and
services without copy-paste drift.
