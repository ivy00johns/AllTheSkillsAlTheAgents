# Contracts Package

This package is the shared language of the platform.

It will define:

- work item schema
- evidence record schema
- contract bundle schema
- run ledger schema
- worker scorecard schema

These contracts should be stable enough that every service can build against
them without reading another service's source code.
